spring:
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/ecom_user}
      database: ecom_user
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8082


management:
  endpoints:
    web:
      exposure:
        include: "*"


app:
  message: "Hello User!"

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true